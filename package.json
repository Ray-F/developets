{
  "name": "project-paws",
  "version": "1.0.0",
  "description": "Project paws project for Dev Hackathon, 2021",
  "main": "index.js",
  "repository": "git@github.com:Ray-F/nfteam-blockchain.git",
  "license": "MIT",
  "private": false,
  "scripts": {
    "start": "yarn run dev",
    "dev": "concurrently \"cd client && yarn run start\" \"cd server && yarn run dev\"",
    "init": "concurrently \"cd client && yarn install\" \"cd server && yarn install\""
  },
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
